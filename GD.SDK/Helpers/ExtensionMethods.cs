using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace WebBDH.Helpers
{
    public static class ExtensionMethods
    {
        //public static IEnumerable<AccountAdmin> AdminWithoutPasswords(this IEnumerable<AccountAdmin> users)
        //{
        //    if (users == null) return null;
        //    return users.Select(x => x.AdminWithoutPassword());
        //}
        //public static AccountAdmin AdminWithoutPassword(this AccountAdmin user)
        //{
        //    if (user == null) return null;
        //    user.Password = null;
        //    return user;
        //}
        //public static AccountView AdminWithoutPasswordView( AccountView user)
        //{
        //    if (user == null) return null;
        //    user.Password = null;
        //    return user;
        //}
        //public static IEnumerable<UserAccount> UserWithoutPasswords(this IEnumerable<UserAccount> users)
        //{
        //    if (users == null) return null;
        //    return users.Select(x => x.UserWithoutPassword());
        //}
        //public static UserAccount UserWithoutPassword(this UserAccount user)
        //{
        //    if (user == null) return null;
        //    user.UserPassword = null;
        //    return user;
        //}
    }
}
